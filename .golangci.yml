version: "2"

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - gosec
    - bodyclose

formatters:
  enable:
    - gofmt
    - goimports

run:
  timeout: 5m
